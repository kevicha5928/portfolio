import styled from 'styled-components';
import { color, font, mixin } from 'Shared/utils/styles';

export const Line = styled.hr`
  border-top: 2px solid ${color.backgroundLight};
  border-radius: 0.5px;
`;
